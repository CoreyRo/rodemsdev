language: node_js
node_js: stable

# safelist
branches:
  only:
  - master

cache:
  yarn: true
  directories:
    - $TRAVIS_BUILD_DIR/source/rodemsdev.ui/node_modules

script: 
  - cd $TRAVIS_BUILD_DIR/source/rodemsdev.ui 
  - yarn test
  - yarn deploy